name: macOS RustDesk - Abdulrahman GUI Test

on:
  workflow_dispatch:

jobs:
  rustdesk:
    runs-on: macos-latest
    timeout-minutes: 360

    steps:

      # =========================================================
      # 1. macOS Information
      # =========================================================
      - name: macOS Information
        shell: bash
        run: |
          set -e

          echo "=========================================="
          echo " macOS Runner Information"
          echo "=========================================="

          echo ""
          echo "macOS:"
          sw_vers

          echo ""
          echo "Architecture:"
          uname -m

          echo ""
          echo "Actions user:"
          whoami

          echo ""
          echo "Console user:"
          stat -f "%Su" /dev/console

          echo ""
          echo "Logged in users:"
          who || true


      # =========================================================
      # 2. Create Abdulrahman
      # =========================================================
      - name: Create macOS User
        shell: bash
        env:
          MACOS_USERNAME: ${{ secrets.MACOS_USERNAME }}
          MACOS_PASSWORD: ${{ secrets.MACOS_PASSWORD }}

        run: |
          set -e

          echo "=========================================="
          echo " Creating macOS User"
          echo "=========================================="

          if [ -z "$MACOS_USERNAME" ]; then
            echo "ERROR: MACOS_USERNAME secret is empty."
            exit 1
          fi

          if [ -z "$MACOS_PASSWORD" ]; then
            echo "ERROR: MACOS_PASSWORD secret is empty."
            exit 1
          fi

          if [[ ! "$MACOS_USERNAME" =~ ^[a-zA-Z0-9._-]+$ ]]; then
            echo "ERROR: Invalid username."
            exit 1
          fi

          echo ""
          echo "Target username:"
          echo "$MACOS_USERNAME"

          if id "$MACOS_USERNAME" >/dev/null 2>&1; then
            echo ""
            echo "User already exists."
          else
            echo ""
            echo "Creating user..."

            sudo sysadminctl \
              -addUser "$MACOS_USERNAME" \
              -fullName "$MACOS_USERNAME" \
              -password "$MACOS_PASSWORD"

            echo "User created."
          fi

          echo ""
          echo "Adding user to admin group..."

          sudo dseditgroup \
            -o edit \
            -a "$MACOS_USERNAME" \
            -t user \
            admin

          echo ""
          echo "User information:"
          id "$MACOS_USERNAME"

          echo ""
          echo "Admin groups:"
          id -Gn "$MACOS_USERNAME"


      # =========================================================
      # 3. Configure Auto Login
      # =========================================================
      - name: Configure Auto Login
        shell: bash
        env:
          MACOS_USERNAME: ${{ secrets.MACOS_USERNAME }}
          MACOS_PASSWORD: ${{ secrets.MACOS_PASSWORD }}

        run: |
          set -e

          echo "=========================================="
          echo " Configuring Auto Login"
          echo "=========================================="

          echo ""
          echo "Console user BEFORE:"
          stat -f "%Su" /dev/console

          echo ""
          echo "Setting auto-login user:"
          echo "$MACOS_USERNAME"

          sudo sysadminctl \
            -autologin set \
            -userName "$MACOS_USERNAME" \
            -password "$MACOS_PASSWORD" || true

          echo ""
          echo "Auto-login status:"

          sudo sysadminctl -autologin status || true

          echo ""
          echo "Console user AFTER configuration:"
          stat -f "%Su" /dev/console


      # =========================================================
      # 4. Verify Abdulrahman Account
      # =========================================================
      - name: Verify Abdulrahman Account
        shell: bash
        env:
          MACOS_USERNAME: ${{ secrets.MACOS_USERNAME }}

        run: |
          set -e

          echo "=========================================="
          echo " Verifying Abdulrahman Account"
          echo "=========================================="

          echo ""
          echo "Username:"
          sudo -u "$MACOS_USERNAME" whoami

          echo ""
          echo "UID:"
          id "$MACOS_USERNAME"

          echo ""
          echo "Groups:"
          id -Gn "$MACOS_USERNAME"

          echo ""
          echo "Home directory:"

          dscl . -read \
            "/Users/$MACOS_USERNAME" \
            NFSHomeDirectory

          echo ""
          echo "Testing HOME environment:"

          sudo -u "$MACOS_USERNAME" -H sh -c '
            echo "whoami=$(whoami)"
            echo "HOME=$HOME"
            echo "USER=$USER"
            echo "LOGNAME=$LOGNAME"
          '

          echo ""
          echo "Current GUI console user:"
          stat -f "%Su" /dev/console


      # =========================================================
      # 5. TEST GUI SESSION AS ABDULRAHMAN
      # =========================================================
      - name: Test Abdulrahman GUI Session
        shell: bash
        env:
          MACOS_USERNAME: ${{ secrets.MACOS_USERNAME }}

        run: |
          set -e

          echo "=========================================="
          echo " Testing Abdulrahman GUI Session"
          echo "=========================================="

          echo ""
          echo "Current console user:"
          stat -f "%Su" /dev/console

          echo ""
          echo "Current GUI sessions:"
          who || true

          echo ""
          echo "User ID:"
          id "$MACOS_USERNAME"

          USER_UID=$(id -u "$MACOS_USERNAME")

          echo ""
          echo "Abdulrahman UID:"
          echo "$USER_UID"

          echo ""
          echo "Home directory:"
          dscl . -read \
            "/Users/$MACOS_USERNAME" \
            NFSHomeDirectory

          echo ""
          echo "=========================================="
          echo " Testing Environment"
          echo "=========================================="

          sudo -u "$MACOS_USERNAME" -H sh -c '
            echo "whoami=$(whoami)"
            echo "id=$(id)"
            echo "HOME=$HOME"
            echo "USER=$USER"
            echo "LOGNAME=$LOGNAME"
          '

          echo ""
          echo "=========================================="
          echo " Testing GUI-capable Process"
          echo "=========================================="

          echo ""
          echo "Attempting to open System Settings as:"
          echo "$MACOS_USERNAME"

          sudo -u "$MACOS_USERNAME" -H \
            env \
            HOME="/Users/$MACOS_USERNAME" \
            USER="$MACOS_USERNAME" \
            LOGNAME="$MACOS_USERNAME" \
            /usr/bin/open -a "System Settings" \
            2>&1 || true

          echo ""
          echo "Waiting for System Settings..."
          sleep 5

          echo ""
          echo "=========================================="
          echo " Processes belonging to Abdulrahman"
          echo "=========================================="

          ps aux | grep "$MACOS_USERNAME" | grep -v grep || true

          echo ""
          echo "=========================================="
          echo " Final Console User"
          echo "=========================================="

          stat -f "%Su" /dev/console

          echo ""
          echo "Logged in users:"
          who || true


      # =========================================================
      # 6. Install RustDesk
      # =========================================================
      - name: Install RustDesk
        shell: bash
        run: |
          set -e

          echo "=========================================="
          echo " Installing RustDesk"
          echo "=========================================="

          brew update

          brew install --cask rustdesk

          if [ ! -d "/Applications/RustDesk.app" ]; then
            echo "ERROR: RustDesk.app was not found."
            exit 1
          fi

          RUSTDESK="/Applications/RustDesk.app/Contents/MacOS/RustDesk"

          if [ ! -f "$RUSTDESK" ]; then
            echo "ERROR: RustDesk executable was not found."
            exit 1
          fi

          echo ""
          echo "RustDesk version:"
          "$RUSTDESK" --version || true


      # =========================================================
      # 7. Configure RustDesk
      # =========================================================
      - name: Configure RustDesk
        shell: bash
        env:
          RUSTDESK_PASSWORD: ${{ secrets.RUSTDESK_PASSWORD }}

        run: |
          set -e

          echo "=========================================="
          echo " Configuring RustDesk"
          echo "=========================================="

          RUSTDESK="/Applications/RustDesk.app/Contents/MacOS/RustDesk"

          if [ -z "$RUSTDESK_PASSWORD" ]; then
            echo "ERROR: RUSTDESK_PASSWORD secret is empty."
            exit 1
          fi

          echo ""
          echo "Getting RustDesk ID..."

          ID=$(
            "$RUSTDESK" --get-id 2>/dev/null || true
          )

          echo "RustDesk ID: $ID"

          echo ""
          echo "Starting RustDesk server..."

          "$RUSTDESK" \
            --server \
            >/tmp/rustdesk-server.log 2>&1 &

          sleep 10

          echo ""
          echo "Setting permanent RustDesk password..."

          PASSWORD_OUTPUT=$(
            sudo "$RUSTDESK" \
              --password "$RUSTDESK_PASSWORD" \
              2>&1
          )

          echo ""
          echo "Password command output:"
          echo "$PASSWORD_OUTPUT"

          if echo "$PASSWORD_OUTPUT" | grep -qi "done"; then
            echo ""
            echo "SUCCESS: Permanent password configured."
          else
            echo ""
            echo "WARNING: RustDesk did not report success."

            echo ""
            echo "RustDesk server log:"
            cat /tmp/rustdesk-server.log || true
          fi

          echo ""
          echo "Restarting RustDesk..."

          pkill -f \
            "/Applications/RustDesk.app/Contents/MacOS/RustDesk" \
            || true

          sleep 5

          nohup "$RUSTDESK" \
            >/tmp/rustdesk.log 2>&1 &

          RUSTDESK_PID=$!

          sleep 10

          if kill -0 "$RUSTDESK_PID" 2>/dev/null; then
            echo ""
            echo "RustDesk is running."
          else
            echo ""
            echo "RustDesk failed to start."

            echo ""
            echo "RustDesk log:"
            cat /tmp/rustdesk.log || true

            exit 1
          fi

          FINAL_ID=$(
            "$RUSTDESK" --get-id 2>/dev/null || true
          )

          echo ""
          echo "=========================================="
          echo " RustDesk Ready"
          echo "=========================================="

          echo ""
          echo "RustDesk ID:"
          echo "$FINAL_ID"

          echo ""
          echo "Permanent password:"
          echo "CONFIGURED"


      # =========================================================
      # 8. محاولة تشغيل RustDesk نفسه تحت Abdulrahman
      # =========================================================
      - name: Test RustDesk As Abdulrahman
        shell: bash
        env:
          MACOS_USERNAME: ${{ secrets.MACOS_USERNAME }}

        run: |
          set -e

          echo "=========================================="
          echo " Testing RustDesk As Abdulrahman"
          echo "=========================================="

          RUSTDESK="/Applications/RustDesk.app/Contents/MacOS/RustDesk"

          echo ""
          echo "Current console user:"
          stat -f "%Su" /dev/console

          echo ""
          echo "Current RustDesk processes:"
          pgrep -fl RustDesk || true

          echo ""
          echo "Starting a RustDesk process as:"
          echo "$MACOS_USERNAME"

          sudo -u "$MACOS_USERNAME" -H \
            env \
            HOME="/Users/$MACOS_USERNAME" \
            USER="$MACOS_USERNAME" \
            LOGNAME="$MACOS_USERNAME" \
            nohup "$RUSTDESK" \
            >/tmp/rustdesk-abdulrahman.log 2>&1 &

          sleep 10

          echo ""
          echo "RustDesk processes:"
          pgrep -fl RustDesk || true

          echo ""
          echo "Processes owned by Abdulrahman:"
          ps aux | grep "$MACOS_USERNAME" | grep RustDesk || true

          echo ""
          echo "RustDesk Abdulrahman log:"
          tail -50 /tmp/rustdesk-abdulrahman.log \
            2>/dev/null || true

          echo ""
          echo "Console user:"
          stat -f "%Su" /dev/console


      # =========================================================
      # 9. Final Status
      # =========================================================
      - name: Final Status
        shell: bash
        env:
          MACOS_USERNAME: ${{ secrets.MACOS_USERNAME }}

        run: |
          echo "=========================================="
          echo " FINAL STATUS"
          echo "=========================================="

          echo ""
          echo "Console user:"
          stat -f "%Su" /dev/console

          echo ""
          echo "Logged in users:"
          who || true

          echo ""
          echo "Actions user:"
          whoami

          echo ""
          echo "Abdulrahman:"
          id "$MACOS_USERNAME"

          echo ""
          echo "RustDesk processes:"
          pgrep -fl RustDesk || true

          echo ""
          echo "RustDesk main log:"
          tail -50 /tmp/rustdesk.log \
            2>/dev/null || true


      # =========================================================
      # 10. Keep Runner Alive
      # =========================================================
      - name: Keep Runner Alive
        shell: bash
        run: |
          echo "=========================================="
          echo " macOS Runner is now running"
          echo "=========================================="

          echo ""
          echo "The GitHub Actions job will remain active."
          echo ""

          while true
          do
            echo ""
            echo "[$(date)] Runner alive"

            echo "Console user:"
            stat -f "%Su" /dev/console \
              2>/dev/null || echo "unknown"

            echo "Logged in users:"
            who || true

            echo "RustDesk:"
            pgrep -fl RustDesk || true

            sleep 300
          done
